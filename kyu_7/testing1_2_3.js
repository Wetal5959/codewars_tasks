


let number = function(array){
    const arrayNew = []
   if(array.length !== 0) {array.forEach(function (element, index){
       arrayNew.push(`${index +1}: ${element}`)
    })}
   return arrayNew;

}

































let number = function(array) {
    if (Array.isArray(array) && array.length !== 0) {
        array.map
    }
}

